{% trans_default_domain 'shop' %}
<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
       aria-expanded="true">
        {{ 'layout.navbar.importantpages'|trans }} <span class="caret"></span>
    </a>
    <ul class="dropdown-menu">
        {% for SPDDM in SPDDMs %}
            <li>
                <a href="{{ path('show_static_page', {'slug' : SPDDM.slug}) }}">
                    {{ SPDDM.title|trans }}
                </a>
            </li>
        {% endfor %}
    </ul>
</li>

