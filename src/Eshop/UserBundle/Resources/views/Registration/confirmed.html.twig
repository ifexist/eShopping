{% extends "@Shop/layout.html.twig" %}
{% trans_default_domain 'ShopBundle' %}

{% block page_content %}
    <div class="container">
        <h2>{{ 'registration.successful'|trans }}</h2>
        {% if targetUrl %}
            <p>
                <a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a>
            </p>
        {% endif %}
    </div>
{% endblock page_content %}
